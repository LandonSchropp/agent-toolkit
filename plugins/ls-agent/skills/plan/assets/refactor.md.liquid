# {{ title }} Refactor

Feature branch: `{{ featureBranch }}`
Base branch: `{{ baseBranch }}`
{% if linearIssueId -%}
Linear Issue ID: `{{ linearIssueId }}`
{% endif -%}

## Overview

<!-- Write a brief description of what code is being refactored. Be succinct. -->

## Context

<!-- Summarize existing work and recent changes on the branch. -->

## Scope

<!-- Define what code is being refactored. Be succinct. -->

## Commits

<!--
Break the refactor into atomic commits using the `ls-git:git-atomic-commit` principles: one logical change per commit, ordered so the tree stays green at every step. Each list item is one commit, in order: bold the commit title, then a colon and what it contains. Delete the placeholder item.
-->

Each item is one atomic commit, in order.

1. **[Commit title]:** [Contents].

## Skills

<!-- The first skill is always required. Add any additional skills that should be invoked when executing this plan. -->

Invoke the following skills:

- `ls-git:git-atomic-commit`
