# Fix {{ title }} Bug

Feature branch: `{{ featureBranch }}`
Base branch: `{{ baseBranch }}`
{% if linearIssueId -%}
Linear Issue ID: `{{ linearIssueId }}`
{% endif -%}
{% if sentryIssueUrl -%}
Sentry Issue: {{ sentryIssueUrl }}
{% endif -%}

## Overview

<!-- Write a brief description of the bug. Be succinct. -->

## Context

<!-- Summarize changes on this branch that may have introduced the bug. -->

## Reproduction Steps

<!-- Describe how to reproduce the bug. If reproduction steps are not yet known, describe what is known about when/how the bug occurs. Be succinct. -->

## Commits

<!--
Break the fix into atomic commits using the `ls-git:git-atomic-commit` principles: one logical change per commit, refactors separated from behavior changes, ordered so the tree stays green at every step. The failing test that reproduces the bug and the fix that makes it pass belong in the same commit; a simple bug is often a single commit. Each list item is one commit, in order: bold the commit title, then a colon and what it contains. Delete the placeholder item.
-->

Each item is one atomic commit, in order.

1. **[Commit title]:** [Contents].

## Skills

<!-- The first skill is always required. Add any additional skills that should be invoked when executing this plan. -->

Invoke the following skills:

- `ls-git:git-atomic-commit`
